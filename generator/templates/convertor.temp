package {{.packageName}}

{{ if not (eq (len .imports) 0) }}
{{ if eq (len .imports) 1 }}
import "{{ index .imports 0 }}"
{{else}}
import (
  {{range $import := .imports}}
    "{{$import}}"
  {{end}}
)
{{end}}
{{end}}

func {{.convertorName}}(from {{.fromName}}) {{.toName}} {
  return {{.toName}}{ {{range $field := .fields}}
      {{$field.ToName}}: {{$field.Conversion}},{{end}}
  }
}
